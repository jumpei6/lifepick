<div class="contents_picks">

  <!-- ***合計金額の表示 -->
  <div id="picks_header-fixed">
    <div id="picks_header-bk">
      <!-- デフォルトは0円 -->
      合計金額：<span id="total_price"></span>円<br>
      <hr>
    </div>
  </div>

  <!-- サイト説明文 -->
  <div id="header">
    あなたが創りたい空間に近い画像を選択してください。<br>
    空間イメージと概算金額が把握できます。
  </div>
  <hr>

  <%= form_tag("/pick", method: :post ) do %>
    <% @options.each do |options| %>
      <div class="clearfix">
        <p><%= options[:title] %></p>
        <% options[:items].each do |items| %>
          <div class="relative">
            <label>
              <input name="<%= items[:category_id] %>" type="radio" class="radio radio_price" data-price="<%= items[:price] %>" checked value="<%= items[:id] %>">
              <img src="<%= items[:image] %>" class="radio_image" height="100" width="161.8">
              <figcaption class="absolute"><p><%= items[:name] %></p></figcaption>
            </label>
          </div>
        <% end %>
      </div>
    <% end %>
    <input type="submit">
  <% end %>

  <!-- メインボタンの表示 -->
  <div class="#">
    <a href="/pick/index" class="square_btn">完成イメージを見る（未作成）</a>
  </div>

  <!-- サブボタンの表示 -->
  <div class="#">
    <a href="/" class="square_sub_btn">TOPに戻る</a>
  </div>

  <!-- app.jsを読み込むために記載 -->
  <script type="text/javascript" src="/assets/lifepick.js"></script>
</div>
