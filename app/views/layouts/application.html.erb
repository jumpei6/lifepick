<!DOCTYPE html>
<html>
<head>
  <title>Lifepick</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- jQueryの読み込み -->
    <!-- jQuery.com/download > Google CDN > 3.x snippet -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

  <!-- materializecssの読み込み-->
    <!-- materializecss.com > Getting Started > CDNをコピペ -->
    <!-- Compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css">

    <!-- Compiled and minified JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>
</head>

<body>
  <!-- 仮置き。一旦コメントアウト -->
  <!--   <header class="header">
    <div class="header__bar row">
      <p class="grid-6"><a href="/">えらべる。</a></p>
      <% if user_signed_in? %>
        <div class="user_nav grid-6">
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
          <a class="post" href="/tweets/new">投稿する</a>
        </div>
      <% else %>
        <div class="grid-6">
          <%= link_to "ログイン", new_user_session_path, class: 'post' %>
          <%= link_to "新規登録", new_user_registration_path, class: 'post' %>
        </div>
      <% end %>
    </div>
  </header> -->

  <%= yield %>

  <footer>
    <!-- <p>
      Copyright Renoveru.Co., Ltd 2018.
    </p> -->
    <!-- 動作確認用にfooterに配置 -->
    <% if user_signed_in? %>
      <div class="user_nav grid-6">
        <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
      </div>
    <% else %>
      <div class="grid-6">
        <%= link_to "ログイン（仮）", new_user_session_path, class: 'post' %>
      </div>
    <% end %>
  </footer>

</body>
</html>
